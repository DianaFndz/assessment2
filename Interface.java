package upgrade;

import java.util.ArrayList;

public interface Interface <T> {
	
	T product(int ind1 , int ind2);
	T maxValue(ArrayList<?> collection);
	T minValue(ArrayList<?> collection);
	T sum(int ind1 , int ind2);
	
	     
}
